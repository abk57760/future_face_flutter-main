<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.example.future_face_app">
    <!-- Flutter needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>


    <!-- <provider android:name="androidx.core.content.FileProvider" android:authorities=""com.example.future_face_app".fileprovider" android:name="Failed to find configured root that contains" android:exported="false" android:grantUriPermissions="true">
        <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/path" />
    </provider> -->
</manifest>
